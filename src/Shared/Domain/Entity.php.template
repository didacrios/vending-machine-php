<?php

declare(strict_types=1);

namespace {{APP_NAMESPACE}}\Shared\Domain;

/**
 * Base Entity
 *
 * Entities are objects that have a distinct identity and lifecycle.
 * They are compared by their identity, not by their attributes.
 */
abstract class Entity
{
    // Base implementation for entities
}
